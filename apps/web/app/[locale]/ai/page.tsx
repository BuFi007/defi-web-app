// Casual agent docs — how to connect an AI agent to the BU.FI MCP + what it can do.
import { AiDocs } from "@/components/ai/docs";

export const metadata = { title: "Agent Docs · BU.FI" };

export default function AiPage() {
  return <AiDocs />;
}
