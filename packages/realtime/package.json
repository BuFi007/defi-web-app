{
  "name": "@bufi/realtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cross-process Redis pub/sub + channel taxonomy for BUFI realtime fan-out. Used by apps/api (WS publish/subscribe) and apps/keeper-perps-matcher (intent-inserted NOTIFY).",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./channels": "./src/channels.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@bufinance/logger": "0.1.1",
    "ioredis": "5.4.1"
  },
  "devDependencies": {
    "bun-types": "1.3.10",
    "typescript": "5"
  }
}
